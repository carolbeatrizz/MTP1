#include <iostream>
#include <stdio.h>
#include <math.h>
using namespace std;

void main()
{
int num,base,logaritmo;
printf("Digite um numero para o calculo: ");
scanf("%d",&num);
printf("Digite um valor para base do calculo: );
scanf("%d",&base);
logaritmo= (log10(num)/log10(base);
printf("O logaritmo do numero na base escolhida vale: %d",logaritmo;
return 0;
}